<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/white</item>
        <item name="colorControlNormal">@color/white</item>

        <!-- Custom widgets example: Material dialog -->
        <item name="md_dark_theme">false</item>
        <item name="md_title_color">@color/primary</item>
        <item name="md_positive_color">@color/primary</item>
        <item name="md_negative_color">@color/gray</item>
        <item name="md_widget_color">@color/primary</item>


        <!-- Actionmode put on action bar -->
        <item name="windowActionModeOverlay">true</item>
        <item name="android:actionModeBackground">@color/primary</item>
    </style>

    <style name="ReserveonEditTextTheme" parent="@style/Theme.AppCompat">
        <item name="android:textColorHint">@color/gray</item>
        <item name="colorAccent">@color/gray</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="android:textColorHighlight">@color/gray</item>
        <item name="android:textColorLink">@color/gray</item>
    </style>

    <style name="ReserveonEditTextStyle" parent="@style/TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/gray</item>
        <item name="colorAccent">@color/gray</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="android:textColorHighlight">@color/gray</item>
        <item name="android:textColorLink">@color/gray</item>
        <item name="android:textColor">@color/gray</item>
    </style>

</resources>
